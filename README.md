# galpopath-data

Static JSON index for Galpopath post filters.

Generated file:

`data/post-index.json`

The GitHub Action updates the JSON once daily and can also be run manually from the Actions tab.

